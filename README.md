# Bank-Management-System-
The Bank Management System is a C-based application that simulates basic banking operations such as account creation, deposit, withdrawal, and balance inquiry. The system uses file handling to store account details permanently. This project demonstrates the use of structures, file handling, and conditional logic in C.
